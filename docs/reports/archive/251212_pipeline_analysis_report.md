# Mouse-FaceLift 파이프라인 분석 보고서

**날짜**: 2024-12-12
**주제**: MVDiffusion + GS-LRM 전체 파이프라인 문제점 분석
**목적**: 생쥐 single-view → 3D 재구성 파이프라인의 이상 결과 원인 파악
**상태**: 분석 완료, 해결 방안 제시

---

## 요약 (Executive Summary)

### 테스트 결과
| 단계 | 모델 | 상태 | 설명 |
|------|------|------|------|
| Multi-view 생성 | MVDiffusion (step 2000) | ✅ **성공** | 단일 뷰에서 6개 다른 각도 뷰 생성 |
| 3D 재구성 | GS-LRM (step 20000) | ⚠️ **부분 실패** | 흐릿하고 불완전한 3D |

### 핵심 문제
1. MVDiffusion 출력 뷰의 **카메라 설정** ≠ GS-LRM 입력 **카메라 파라미터**
2. FaceLift **prompt_embeds**가 인코딩한 카메라 방향과 실제 mouse 카메라 불일치

---

## 1. 테스트 결과 요약

### 1.1 개별 테스트 (성공)
| 단계 | 입력 | 출력 | 결과 |
|------|------|------|------|
| MVDiffusion (validation) | 6x 동일 top-down 뷰 | 6x 다른 각도 뷰 | ✅ PSNR 25.85, SSIM 0.97 |
| GS-LRM (sample data) | 32 view + camera params | 3D Gaussian | ⚠️ 흐릿함 |

### 1.2 전체 파이프라인 테스트 (실패)
| 단계 | 입력 | 출력 | 문제 |
|------|------|------|------|
| MVDiffusion | 측면 뷰 이미지 | 이상한 6 view | ❌ Reference view 불일치 |
| GS-LRM | MVDiff 출력 + FaceLift 카메라 | 기형 3D | ❌ 카메라 파라미터 불일치 |

---

## 2. 발견된 문제점

### 2.1 문제 1: MVDiffusion Reference View 불일치

**원인:**
```
학습 시: reference_view_idx = 0 (top-down 경사 뷰, ~20° elevation)
테스트 시: 임의의 측면 뷰 입력
```

**증거:**
- Validation input: 위에서 내려다 본 마우스 (top-down)
- 테스트 input: 옆에서 본 마우스 (측면 view)
- MVDiffusion은 학습 시 본 reference view 패턴에만 적응됨

**결과:**
- 학습 시 본 각도와 다른 입력 → 생성 품질 급격히 저하
- Multi-view 일관성 붕괴

### 2.2 문제 2: GS-LRM 카메라 파라미터 불일치

**카메라 설정 비교:**

| 항목 | Mouse 학습 데이터 | FaceLift 기본 (test_full_pipeline 사용) |
|------|------------------|----------------------------------------|
| Camera Z | 0.923 (경사) | 0.0 (수평) |
| Elevation | ~20° | 0° |
| 좌표계 방향 | 다름 | 다름 |

**Mouse 학습 데이터 카메라:**
```json
"blender_camera_location": [2.537, 0.0, 0.923]
```

**FaceLift 기본 카메라:**
```json
"blender_camera_location": [-2.7, 0.0, 0.0]
```

**결과:**
- MVDiffusion이 생성한 뷰의 카메라 가정 ≠ GS-LRM에 전달된 카메라
- 3D 재구성 시 위치 오류 → 기형적인 결과

### 2.3 문제 3: 데이터 다양성 부족

**현재 학습 데이터 특성:**
- 단일 마우스 객체
- 고정된 카메라 거리 (~2.7)
- 고정된 elevation (~20°)
- 제한된 포즈 다양성

**영향:**
- Reference view가 학습 시 본 것과 조금만 달라도 성능 급락
- 다양한 거리/각도에서의 일반화 능력 부족

---

## 3. 각 단계별 입출력 데이터 흐름

```
┌─────────────────────────────────────────────────────────────────┐
│                    현재 파이프라인 데이터 흐름                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [입력 이미지]                                                    │
│      │                                                          │
│      │  ⚠️ 문제: 임의 각도의 이미지 입력                           │
│      ▼                                                          │
│  ┌──────────────────┐                                           │
│  │   MVDiffusion    │  학습: top-down view only                 │
│  │   (checkpoint-   │  기대: 같은 각도의 reference view          │
│  │    2000)         │                                           │
│  └────────┬─────────┘                                           │
│           │                                                     │
│           │  출력: 6x views (512x512)                           │
│           │  ⚠️ 문제: reference view 불일치로 이상한 결과          │
│           ▼                                                     │
│  ┌──────────────────┐                                           │
│  │    GS-LRM        │                                           │
│  │   (mouse         │  입력 카메라: FaceLift 기본 (수평)          │
│  │   finetune)      │  학습 카메라: Mouse 데이터 (경사 20°)       │
│  └────────┬─────────┘                                           │
│           │  ⚠️ 문제: 카메라 파라미터 불일치                       │
│           ▼                                                     │
│  [3D Gaussian / PLY / Video]                                    │
│           │                                                     │
│           ▼                                                     │
│  ❌ 기형적인 3D 결과                                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. 카메라 거리 조절 가능성

### 4.1 현재 설정
- **카메라 거리**: 2.7 (고정)
- **FOV**: ~50° (focal length 548.99 @ 512px)

### 4.2 조절 가능한 위치

**MVDiffusion:**
- `mvdiffusion/data/mouse_dataset.py` - 데이터 로딩 시 카메라 정보 사용
- Prompt embeddings에 카메라 정보가 인코딩되어 있을 수 있음

**GS-LRM:**
- `inference_mouse.py:compute_mvdiffusion_cameras()` - 카메라 거리 파라미터 `camera_distance=2.7`
- `utils_folder/opencv_cameras.json` - 카메라 매트릭스 직접 수정

### 4.3 거리 조절 시 고려사항
```python
# inference_mouse.py:93
def compute_mvdiffusion_cameras(
    image_size: int = 512,
    device: str = "cuda",
    camera_distance: float = 2.7  # ← 이 값 조절 가능
) -> Tuple[torch.Tensor, torch.Tensor]:
```

**주의:** 거리만 조절해도 학습-추론 불일치 문제 발생 가능

---

## 5. 해결 방안

### 5.1 단기 해결책 (즉시 적용 가능)

1. **올바른 카메라 파라미터 사용**
   ```python
   # test_full_pipeline.py에서 FaceLift 카메라 대신 Mouse 카메라 사용
   --camera_json data_sample/gslrm/sample_000/opencv_cameras.json
   ```

2. **올바른 Reference View 사용**
   - 테스트 시 top-down view 이미지만 입력
   - Validation과 동일한 각도의 이미지 사용

3. **카메라 설정 통일**
   - MVDiffusion과 GS-LRM이 같은 카메라 설정 공유하도록 수정

### 5.2 중기 해결책 (데이터/학습 개선)

1. **다양한 Reference View 학습**
   - 현재: reference_view_idx = 0 (top-down only)
   - 개선: 모든 6개 뷰를 번갈아 reference로 사용

   ```yaml
   # mouse_mvdiffusion.yaml
   reference_view_idx: random  # 또는 [0, 1, 2, 3, 4, 5]
   ```

2. **더 다양한 데이터 수집**
   - 다양한 마우스 개체
   - 다양한 포즈
   - 다양한 카메라 거리/각도

### 5.3 장기 해결책 (아키텍처 개선)

1. **카메라 조건부 생성**
   - 입력 이미지의 카메라 포즈 추정
   - 추정된 포즈를 조건으로 MVDiffusion 구동

2. **스케일/거리 불변 학습**
   - 다양한 스케일의 데이터로 augmentation
   - 거리에 robust한 representation 학습

---

## 6. 즉시 테스트 권장 사항

### 6.1 올바른 파이프라인 테스트
```bash
# Mouse 카메라 파라미터와 올바른 reference view 사용
python test_full_pipeline.py \
    --input_image checkpoints/mvdiffusion/mouse/val/global_step_2000/0000/input.png \
    --camera_json data_sample/gslrm/sample_000/opencv_cameras.json \
    --output_dir outputs/test_correct_camera
```

### 6.2 카메라 파라미터 생성 스크립트 필요
- MVDiffusion 출력에 맞는 카메라 생성
- Mouse 데이터의 실제 카메라 설정 반영

---

## 7. 결론

### 핵심 원인
1. **Reference view 불일치**: MVDiffusion이 특정 각도로만 학습됨
2. **카메라 파라미터 불일치**: MVDiffusion과 GS-LRM 간 카메라 설정 불일치
3. **데이터 다양성 부족**: 단일 reference view, 고정 거리/각도

### 우선순위
1. 🔴 **긴급**: 카메라 파라미터 통일 (즉시)
2. 🟡 **중요**: 다양한 reference view 학습 (추가 학습 필요)
3. 🟢 **개선**: 더 많은 데이터 수집 (장기)

---

## 8. 최종 테스트 결과 (실제 마우스 데이터)

### 8.1 테스트 조건
```bash
python test_full_pipeline.py \
    --input_image data_mouse/sample_000000/images/cam_000.png \
    --camera_json data_mouse/sample_000000/opencv_cameras.json \
    --output_dir outputs/test_real_mouse
```

### 8.2 결과 분석

**MVDiffusion 출력 (multiview_grid.png):**
- ✅ 입력 마우스 (서 있는 포즈)에서 6개 다른 각도 뷰 생성
- ✅ 각 뷰가 자연스럽게 회전된 모습
- ✅ 포즈와 텍스처 일관성 유지

**GS-LRM 출력 (render_view_*.png):**
- ⚠️ render_view_00: 희미하고 흐릿한 마우스 형태
- ❌ render_view_02: 거의 보이지 않음
- ❌ 3D 재구성 품질 불량

### 8.3 GS-LRM 실패 원인 분석

**원인 1: 카메라 좌표계 불일치**
```
MVDiffusion prompt_embeds: FaceLift 기본 카메라 (수평, elevation=0°)
Mouse 데이터 카메라: 경사 카메라 (elevation≈20°)
```
→ MVDiffusion이 생성한 뷰의 실제 카메라 ≠ GS-LRM에 전달한 카메라

**원인 2: prompt_embeds의 카메라 인코딩**
- `mvdiffusion/data/fixed_prompt_embeds_6view/clr_embeds.pt`
- FaceLift 원본: 수평면 6방향 (0°, 60°, 120°, 180°, 240°, 300°)
- Mouse 학습: 경사면 6방향
- **불일치**: MVDiffusion은 FaceLift prompt_embeds 그대로 사용 → 수평면 기준 생성

---

## 9. 해결 방안 (Action Items)

### 9.1 즉시 해결 가능 (Hot Fix)

**Option A: GS-LRM에 FaceLift 카메라 사용**
```bash
# MVDiffusion 출력이 FaceLift prompt_embeds 기준이므로
python test_full_pipeline.py \
    --input_image data_mouse/sample_000000/images/cam_000.png \
    --camera_json utils_folder/opencv_cameras.json  # FaceLift 카메라!
```

**Option B: Mouse용 prompt_embeds 생성**
```python
# mouse 카메라 설정에 맞는 새 prompt_embeds 생성 필요
# 현재는 FaceLift 기본 사용 중
```

### 9.2 중기 해결 (추가 학습 필요)

1. **Mouse 전용 prompt_embeds 생성 및 MVDiffusion 재학습**
   - Mouse 카메라 설정에 맞는 방향 임베딩 생성
   - 또는 camera-conditioned generation 구현

2. **다양한 Reference View 학습**
   ```yaml
   # mouse_mvdiffusion.yaml
   reference_view_idx: [0, 1, 2, 3, 4, 5]  # 모든 뷰를 reference로
   ```

3. **데이터 다양성 증가**
   - 더 많은 마우스 개체
   - 다양한 포즈
   - 다양한 거리/각도

### 9.3 카메라 거리 조절 방법

**MVDiffusion에서:**
- prompt_embeds에 인코딩된 카메라 거리는 고정 (학습 시 설정)
- 변경하려면 새 prompt_embeds 생성 필요

**GS-LRM에서:**
```python
# inference_mouse.py:93
def compute_mvdiffusion_cameras(
    camera_distance: float = 2.7  # 이 값 조절
)

# 또는 opencv_cameras.json 직접 수정
# blender_camera_location의 거리 = sqrt(x² + y² + z²) ≈ 2.7
```

**주의:** 거리 변경 시 학습-추론 불일치로 품질 저하 가능

---

## 10. 최종 실험 비교 결과

### 10.1 GS-LRM 모델 비교

| 테스트 | MVDiffusion | GS-LRM | 카메라 | 결과 |
|--------|-------------|--------|--------|------|
| test_real_mouse | mouse (step 2000) | **mouse_finetune** | mouse | ❌ 흐릿 |
| test_facelift_camera | mouse (step 2000) | **mouse_finetune** | FaceLift | ❌ 흐릿 |
| **test_gslrm_pretrained** | mouse (step 2000) | **pretrained (human)** | FaceLift | ✅ **선명!** |

### 10.2 원인 분석

```
┌─────────────────────────────────────────────────────────────────────┐
│  MVDiffusion 출력 → FaceLift prompt_embeds 기준 (수평 6방향)         │
│                                                                     │
│  GS-LRM pretrained (human): FaceLift 카메라로 학습 → 일치 ✅         │
│  GS-LRM mouse_finetune: Mouse 카메라 (경사 20°)로 학습 → 불일치 ❌    │
└─────────────────────────────────────────────────────────────────────┘
```

### 10.3 학습 시 카메라 파라미터 역할

| 모델 | 카메라 사용 방식 | 변경 가능성 |
|------|-----------------|-------------|
| MVDiffusion | prompt_embeds에 암묵적 인코딩 | 재학습 필요 |
| GS-LRM | 명시적 입력 (c2w, intrinsics) | 추론 시 변경 가능, 단 학습과 일치해야 |

---

## 11. 결론 및 권장사항

### 현재 작동하는 파이프라인
```bash
# MVDiffusion (mouse) + GS-LRM (pretrained human) + FaceLift 카메라
python test_full_pipeline.py \
    --input_image <mouse_image.png> \
    --camera_json utils_folder/opencv_cameras.json \
    --gslrm_checkpoint checkpoints/gslrm/ckpt_0000000000021125.pt
```

### GS-LRM mouse_finetune 재학습 필요성

**결론: 현재로서는 재학습 불필요**

이유:
1. MVDiffusion이 FaceLift prompt_embeds 사용 → 수평 뷰 출력
2. GS-LRM pretrained가 같은 카메라 설정 → 잘 작동
3. mouse_finetune은 다른 카메라 → 불일치

**만약 재학습 한다면**:
- GS-LRM을 **FaceLift 카메라** 설정으로 재학습
- 또는 Mouse 카메라용 **새 prompt_embeds** 생성 + MVDiffusion 재학습

### 다음 단계 권장
1. **즉시**: GS-LRM pretrained로 더 많은 마우스 샘플 테스트
2. **단기**: MVDiffusion 학습 계속 (현재 step 2000 → 더 많은 step)
3. **선택적**: 카메라 설정 통일 후 전체 파이프라인 재학습

---

## 첨부: 테스트 결과 이미지

| 파일 | 설명 | 위치 |
|------|------|------|
| input.png | 실제 마우스 입력 (서 있는 포즈) | outputs/test_real_mouse/ |
| multiview_grid.png | MVDiffusion 6뷰 생성 ✅ | outputs/test_real_mouse/ |
| render_view_*.png | GS-LRM 렌더링 (품질 문제) | outputs/test_real_mouse/ |
| turntable.mp4 | 360° 회전 영상 | outputs/test_real_mouse/ |
| val/pred_gs_3.0.png | MVDiffusion Validation 결과 | outputs/mvdiffusion_val/ |
