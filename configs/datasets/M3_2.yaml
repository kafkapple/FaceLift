# =============================================================================
# Dataset: M3_2
# =============================================================================
# Per-sample zoom, PP=256 (RECOMMENDED)
# Category: production
# Status: RECOMMENDED
# =============================================================================

_name: M3_2
_description: "Per-sample zoom, PP=256 (RECOMMENDED)"
_category: production
_status: RECOMMENDED

# Preprocessing info (reference only, actual preprocessing uses presets.py)
_preprocessing:
  preset: M3_2
  transform: homography
  pp: 256
  fx: 549
  zoom_scope: per_sample
  zoom_range: [1.0, 1.8]
  safe_zoom: false
  pp_correction: false

# Dataset paths
training:
  dataset:
    dataset_path: ~/data/preprocessed/FaceLift_mouse/M3_2/data_mouse_train.txt

validation:
  dataset_path: ~/data/preprocessed/FaceLift_mouse/M3_2/data_mouse_val.txt
