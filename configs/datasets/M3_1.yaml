# =============================================================================
# Dataset: M3_1
# =============================================================================
# Global zoom, PP=256 (stable)
# Category: production
# Status: ACTIVE
# =============================================================================

_name: M3_1
_description: "Global zoom, PP=256 (stable)"
_category: production
_status: ACTIVE

# Preprocessing info (reference only, actual preprocessing uses presets.py)
_preprocessing:
  preset: M3_1
  transform: homography
  pp: 256
  fx: 549
  zoom_scope: global
  zoom_range: [1.0, 1.8]
  safe_zoom: false
  pp_correction: false

# Dataset paths
training:
  dataset:
    dataset_path: /home/joon/data/preprocessed/FaceLift_mouse/M3_1/data_mouse_train.txt

validation:
  dataset_path: /home/joon/data/preprocessed/FaceLift_mouse/M3_1/data_mouse_val.txt
