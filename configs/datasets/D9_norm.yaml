# Dataset: D9_norm
# Original resolution with translation normalization only
# NOTE: Run preprocessing first: python -m mouse_extensions.preprocessing.preprocess --preset D9_norm ...

_name: D9_norm
_description: "Original resolution + translation normalization (experimental)"
_category: native
_preprocessing:
  paradigm: native
  transform: none
  normalize_translation: true
  target_distance: 2.7
  output_size: original

training:
  dataset:
    dataset_path: /home/joon/data/preprocessed/FaceLift_mouse/D9_norm/data_mouse_train.txt

validation:
  dataset_path: /home/joon/data/preprocessed/FaceLift_mouse/D9_norm/data_mouse_val.txt

_stats:
  num_train: 0  # Update after preprocessing
  num_val: 0
