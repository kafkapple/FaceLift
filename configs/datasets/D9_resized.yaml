# Dataset: D9_resized
# D9 data resized to 512x512 with full normalization (recommended for pretrained compatibility)
# NOTE: Run preprocessing first: python -m mouse_extensions.preprocessing.preprocess --preset D9_resized ...

_name: D9_resized
_description: "D9 resized to 512x512 with full normalization (safe)"
_category: pp_centered_shift
_preprocessing:
  paradigm: pp_centered_shift
  transform: affine
  scale_mode: individual
  target_fx: 549.0
  output_size: 512

training:
  dataset:
    dataset_path: /home/joon/data/preprocessed/FaceLift_mouse/D9_resized/data_mouse_train.txt

validation:
  dataset_path: /home/joon/data/preprocessed/FaceLift_mouse/D9_resized/data_mouse_val.txt

_stats:
  num_train: 0  # Update after preprocessing
  num_val: 0
