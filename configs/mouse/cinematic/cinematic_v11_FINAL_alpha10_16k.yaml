# FINAL alpha=1.0 sharper supervision variant (16k count-matched)
extends: cinematic_v11.yaml

model:
  # ⚠️ 260814: 이 변형의 gaussian 디렉토리는 서버 정리 대상이라 실물이 없을 수 있다.
  # 재생성 = stage1(batch_ply_export, ckpt=M5t2_6view_alpha10_v3) → stage3(make_maskcarve16k).
  # 절차 정본 = docs/theory/GAUSSIAN_FILTERING_THEORY.md §3.
  ply_dir: ${FL_GAUSSIAN_ROOT}/M5t2_6view_alpha10_v3_maskcarve16k

global:
  fps: 20
  resolution: 1024
  gt_auto_trim_padding: true
  gt_trim_pad_extra: 4
