# FINAL filtered/a0.067 variant (~11k Gaussians, opacity threshold)
extends: cinematic_v11.yaml

model:
  # ⚠️ 260814: 이 변형의 gaussian 디렉토리는 서버 정리 대상이라 실물이 없을 수 있다.
  # 재생성 = stage1(batch_ply_export) → stage2(filter_ply --opacity_threshold 0.067).
  # 절차 정본 = docs/theory/GAUSSIAN_FILTERING_THEORY.md §3.
  ply_dir: ${FL_GAUSSIAN_ROOT}/filtered/a0.067

global:
  fps: 20
  resolution: 1024
  gt_auto_trim_padding: true
  gt_trim_pad_extra: 4
