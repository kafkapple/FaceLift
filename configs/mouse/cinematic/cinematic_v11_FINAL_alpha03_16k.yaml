# FINAL paper preset — alpha=0.3, 16k count-matched (paper exact)
# All v5 fixes inherited (face_follow upstream smoothing + reverse show-through)
# 1024px / 20fps real-time
extends: cinematic_v11.yaml

model:
  # 260814: 구 경로 /node_data/joon/data/shared/... 는 개인 계정 경유라 오프보딩과 함께 죽는다.
  # FL_GAUSSIAN_ROOT 예시:
  #   win WSL  /mnt/d/data/derived/mac_backups_260813/gpu03_offserver_260813/FaceLift_gaussians_260814
  #   gpu03    /node_data_2/dataset/animal_behavior/FaceLift_mouse_6view/gaussians
  ply_dir: ${FL_GAUSSIAN_ROOT}/M5t2_6view_alpha03_v3_maskcarve16k

global:
  fps: 20
  resolution: 1024
  gt_auto_trim_padding: true
  gt_trim_pad_extra: 4
