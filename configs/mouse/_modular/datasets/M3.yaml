# =============================================================================
# Dataset: M3 (Coverage-based Zoom - D10.3 preset)
# =============================================================================

_dataset:
  name: M3
  description: "Coverage 4.55%, offset 48px, precision_homography, verified"
  path: /home/joon/data/preprocessed/FaceLift_mouse/M3
  split: "90:10 (train:val)"
  status: "VERIFIED"

training:
  dataset:
    dataset_path: /home/joon/data/preprocessed/FaceLift_mouse/M3/data_mouse_train.txt

validation:
  enabled: true
  dataset_path: /home/joon/data/preprocessed/FaceLift_mouse/M3/data_mouse_val.txt
  val_every: 200

inference:
  enabled: false
