# =============================================================================
# Experiment: E1.1 Paper Baseline (Random)
# =============================================================================
# Hypothesis H1: Baseline performance measurement
# 4 views, random selection, no mask supervision
# =============================================================================

_name: E1_1_paper_random
_description: "Paper baseline with random view selection"
_hypothesis: H1
_comparison_group: [E1_1, E1_2]

training:
  dataset:
    num_views: 6
    num_input_views: 4
    random_view_selection: true

  losses:
    masked_pixelalign_loss: false
    masked_l2_loss: false
    masked_ssim_loss: false
    mask_mode: none
    alpha_loss_weight: 0.0

  logging:
    wandb:
      exp_name: null  # Auto-generated as {dataset}_{experiment}
