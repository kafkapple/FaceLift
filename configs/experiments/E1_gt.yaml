# E1: GT Mask Only - Pose Splatter Style
# mask_mode: gt, normalize_by_mask for small foreground
# Reference: Pose Splatter (normalized masked loss)

training:
  losses:
    mask_mode: gt
    normalize_by_mask: true
    alpha_loss_weight: 0.0
    bg_loss_weight: 0.0
