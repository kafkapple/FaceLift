# E0: Baseline (No Mask) - GS-LRM Paper
# mask_mode: none, no alpha supervision, no bg penalty
# Reference: Original GS-LRM paper settings

training:
  losses:
    mask_mode: none
    normalize_by_mask: false
    alpha_loss_weight: 0.0
    bg_loss_weight: 0.0
