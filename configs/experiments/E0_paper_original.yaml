# =============================================================================
# E0_1: Paper Original (Baseline)
# =============================================================================
# GS-LRM / FaceLift 논문 원본 설정
# Base config가 이미 논문 설정이므로 최소한만 명시

model:
  num_views: 6
  num_input_views: 4

training:
  losses:
    mask_mode: none
    alpha_loss_weight: 0.0
    bg_loss_weight: 0.0
