# E2 variant: 5-view input (maximum information)
# 5 input views, 1 holdout view for supervision

model:
  num_input_views: 5

training:
  dataset:
    random_view_selection: true

  losses:
    mask_mode: gt
    alpha_loss_weight: 0.1
    alpha_loss_type: mse
