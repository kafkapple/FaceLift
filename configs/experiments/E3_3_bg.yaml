# E4: Background Penalty - Object-Centric 2DGS Style
# Alpha supervision + background loss penalty
# Reference: Object-Centric 2DGS

training:
  losses:
    mask_mode: none
    normalize_by_mask: false
    alpha_loss_weight: 0.1
    alpha_loss_type: mse
    bg_loss_weight: 0.5
