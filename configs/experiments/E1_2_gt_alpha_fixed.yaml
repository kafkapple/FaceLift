# E2 variant: Fixed view selection (not random)
# Same as E2_gt_alpha but with fixed view order

training:
  dataset:
    random_view_selection: false

  losses:
    mask_mode: gt
    alpha_loss_weight: 0.1
    alpha_loss_type: mse
