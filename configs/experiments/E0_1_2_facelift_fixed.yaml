# =============================================================================
# E0_1_2_facelift_fixed: FaceLift + Fixed View Selection
# =============================================================================
# E0_1_facelift 변형: 고정 뷰 선택

model:
  num_views: 6
  num_input_views: 4

training:
  dataset:
    random_view_selection: false

  losses:
    mask_mode: none
    alpha_loss_weight: 0.0
    bg_loss_weight: 0.0
