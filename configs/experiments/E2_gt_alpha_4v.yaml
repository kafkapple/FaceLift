# E2 variant: 4-view ablation
# Same as E2_gt_alpha but with 4 input views

training:
  dataset:
    num_input_views: 4

  losses:
    mask_mode: gt
    alpha_loss_weight: 0.1
    alpha_loss_type: mse
