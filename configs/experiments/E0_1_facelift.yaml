# =============================================================================
# E0_1_facelift: FaceLift Paper Original (Baseline)
# =============================================================================
# GS-LRM / FaceLift 논문 원본 설정
# - mask_mode: none
# - alpha_loss: 0.0

model:
  num_views: 6
  num_input_views: 4

training:
  dataset:
    random_view_selection: true

  losses:
    mask_mode: none
    alpha_loss_weight: 0.0
    bg_loss_weight: 0.0
