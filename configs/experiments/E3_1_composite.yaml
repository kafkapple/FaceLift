# E5: Composite Background - Nerfstudio Style
# Render with learned background, composite with GT
# Reference: Nerfstudio

training:
  losses:
    mask_mode: composite
    normalize_by_mask: false
    alpha_loss_weight: 0.05
    alpha_loss_type: mse
    bg_loss_weight: 0.0
